name: backtest
on:
  push:
  schedule:
    - cron: '0 11 * * *'
jobs:
  backtest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - run: pip install -r requirements.txt
      - run: python backtest_ag5_v16_3_core.py
      - run: python backtest_ag5_v15_1_core.py
      - run: python backtest_ag5_v16_0_institutional.py
      - run: python backtest_ag5_v16_0_c_lite.py
      - run: cat HISTORY_AG5_CORE.csv
      - run: cat HISTORY_AG5_V15_1_CORE.csv
      - run: cat HISTORY_AG5_V16_0.csv
      - run: cat HISTORY_AG5_V16_0_C_LITE.csv
      - name: save history
        run: |
          git config user.name "bot"
          git config user.email "bot@bot.com"
          git add HISTORY_*.csv || true
          git commit -m "daily log v16 lite" || true
          git push || true
